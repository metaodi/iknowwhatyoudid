# Spec: Example Feature

> Template. Replace this content when starting a real feature. Keep the
> section headings so specs stay consistent and easy to review.

## Summary

One or two sentences describing the feature and the problem it solves.

## Motivation / Why

Why does this feature matter to the user? What problem does it solve, or
what value does it add? Reference the project constitution
(`.specify/memory/constitution.md`) if relevant.

## User Stories

- As a `<user>`, I want `<capability>`, so that `<benefit>`.

## Requirements

Describe *what* the feature must do, not *how* it will be built.

- Requirement 1
- Requirement 2

## Out of Scope

List anything explicitly excluded from this feature, to avoid scope creep.

## Open Questions

List any ambiguities to resolve during the clarification pass, before
planning begins.
